global:
  scrape_interval: 15s

scrape_configs:
  - job_name: expocraft-backend
    metrics_path: /metrics/prometheus
    static_configs:
      - targets:
          - 127.0.0.1:4000

rule_files:
  - alerts.yml
