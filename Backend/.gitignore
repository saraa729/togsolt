node_modules/
.env
.DS_Store

# Демо өгөгдөл (`data/expo-store.json`, `data/uploads/`) нь ЗОРИУДААР
# хянагддаг: шинэ компьютер дээр clone хийхэд каталог зурагтайгаа шууд
# ажиллана. Тухайн файл хүсэлт бүрд шинэчлэгддэг тул `git status`-д
# байнга өөрчлөлт харагдана — энэ нь хэвийн.
# Хэрэв хянахыг хүсэхгүй бол доорх хоёр мөрийн тайлбарыг ав:
# data/expo-store.json
# data/uploads/
